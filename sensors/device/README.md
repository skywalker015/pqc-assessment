# Device Sensor Agent

Local device inspection agent for PQC configuration checks.

Planned responsibilities:
- OpenSSL and OpenSSH version checks
- Public key inspection
- SSH daemon configuration review
- CSV export in supervised mode
- Direct backend reporting in unsupervised mode
