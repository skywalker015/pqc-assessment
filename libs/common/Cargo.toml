[package]
name = "pqc-common"
version = "0.1.0"
edition = "2021"

[lib]
name = "pqc_common"
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
